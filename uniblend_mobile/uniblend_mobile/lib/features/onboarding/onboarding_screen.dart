import 'package:flutter/material.dart';

class UniBlendOnboarding extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Onboarding')),
      body: Center(
        child: Text('Welcome to UniBlend!'),
      ),
    );
  }
}
